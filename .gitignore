# dependencies
node_modules

# build output (собирается в CI, не храним в репозитории)
dist

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.vscode
.idea
*.local
.vite

# боевой конфиг платежей — генерируется в CI из секретов, в репозитории не хранится
public/api/config.php
public/api/logs/*.log
public/api/cache/lots.json
